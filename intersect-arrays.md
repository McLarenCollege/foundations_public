Write a function called `intersectArrays` that accepts two arrays and returns a new array which contains only the elements that are present in both the arrays.

```js
function intersectArrays(arr1, arr2){
  // Write your code here
}

console.log(intersectArrays([2, 4, 7, 9], [1, 2, 4, 5, 7])); // should print [2, 4, 7]
console.log(intersectArrays([1, 2, 3, 4], [5, 6, 7, 8])); // should print []
```
