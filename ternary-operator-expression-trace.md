Guess the output for the following piece of code by writing the expression trace.

```js
let alf = 5;
let soo = "max";
let max = 7 ;
let key = 2 % alf;
let res = (max % 3) >= (alf + max % key) ? key % (alf * 3) : soo + "Hello" ;
console.log(res);
```
